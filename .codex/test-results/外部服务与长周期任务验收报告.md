# 外部服务与长周期任务验收报告

## 1. 执行信息

- 执行日期：2026-06-14
- 手机号：`193****0949`
- 邮箱：`311***@qq.com`
- 执行方式：显式开启 `ExternalAndScheduledAcceptanceTest`
- 数据安全：临时测试数据执行后全部恢复，数据库完整性复查通过

## 2. 验收结果

| 项目 | 结果 | 说明 |
|---|---|---|
| 真实邮件 | 通过 | SMTP 接受发送请求，测试验证码为 `246810`；最终投递需在 QQ 邮箱确认 |
| 真实短信 | 通过 | 已切换号码认证 `dypnsapi` SDK，阿里云响应 `OK`，验证码 `246810` |
| OSS 上传 | 通过 | 成功上传测试对象 |
| OSS 预签名下载 | 通过 | 成功读取并核对对象内容 |
| OSS 删除 | 通过 | 清理任务成功删除真实 OSS 对象 |
| 维修周期 3/7 天提醒 | 通过 | 通知正确且重复执行保持幂等 |
| 待确认 3/7/27 天提醒 | 通过 | 三类提醒正确且重复执行保持幂等 |
| 待确认 30 天自动完成 | 通过 | 工单、日志、系统评论、通知与操作日志正确生成 |
| 30 天草稿清理 | 通过 | 超期草稿正确逻辑删除 |
| 人工恢复过期任务 | 通过 | 已通过且超期记录正确变为已过期 |
| 附件冗余清理 | 通过 | 修复后 OSS 对象和数据库逻辑删除状态一致 |

## 3. 发现并修复的问题

### BUG-OSS-001

- 级别：P1
- 问题：附件清理任务删除真实 OSS 对象并写入 `SUCCESS`，但使用 `updateById` 修改 MyBatis-Plus 逻辑删除字段无效，数据库附件仍保持未删除。
- 修复：先写入 OSS 清理成功状态，再使用 `deleteById` 执行标准逻辑删除。
- 回归：真实 OSS 上传、下载、任务删除及数据库状态验证全部通过。

## 4. 短信 SDK 修正

- 原实现错误使用普通短信 `dysmsapi` SDK，与现有号码认证短信配置不匹配。
- 已切换为 `com.aliyun:dypnsapi20170525:2.0.0`。
- 已兼容 `ALIYUN_SMS_*` 与 `SMS_*` 环境变量名称。
- 已检查阿里云响应体，仅 `code=OK` 时判定发送成功。
- 已使用号码认证模板 `100001` 向 `193****0949` 完成真实发送。

测试类默认跳过，只有设置 `EXTERNAL_ACCEPTANCE_ENABLED=true` 才会触发真实发送。

## 5. 可重复执行命令

```powershell
$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::new()
$env:EXTERNAL_ACCEPTANCE_ENABLED = 'true'
$env:EXTERNAL_TEST_PHONE = '目标手机号'
$env:EXTERNAL_TEST_EMAIL = '目标邮箱'
.\mvnw.cmd -Dtest=ExternalAndScheduledAcceptanceTest test
```

## 6. 最终结论

真实短信、邮件 SMTP、真实 OSS 与全部长周期任务均通过本地联调验收。
